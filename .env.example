# ==============================================================================
# JARVIS — Environment Variables Template
# Copy this file to .env and adjust values as needed.
# NEVER commit .env with secrets or credentials to version control.
# ==============================================================================

# System environment (development, production, testing)
JARVIS_ENV=development

# Operating mode (default: terminal)
JARVIS_MODE=terminal

# Debug mode (true / false)
JARVIS_DEBUG=false

# Logging verbosity (DEBUG, INFO, WARNING, ERROR, CRITICAL)
JARVIS_LOG_LEVEL=INFO

# Model configuration (hybrid, groq, gemini, ollama, mock)
JARVIS_MODEL_PROVIDER=hybrid
JARVIS_MODEL_NAME=llama-3.3-70b-versatile
JARVIS_MODEL_BASE_URL=http://localhost:11434
JARVIS_MODEL_TEMPERATURE=0.7
JARVIS_MODEL_FALLBACK=true

# Cloud LLM API Keys (Free Tier: get key at https://console.groq.com)
GROQ_API_KEY=
GEMINI_API_KEY=

# Personality configuration
JARVIS_PERSONALITY_MODE=normal
JARVIS_PERSONALITY_HUMOR=0.6
JARVIS_PERSONALITY_SARCASM=0.4
JARVIS_PERSONALITY_FORMALITY=0.7
JARVIS_PERSONALITY_WARMTH=0.6
JARVIS_PERSONALITY_VERBOSITY=0.4
JARVIS_PERSONALITY_CONFIDENCE=0.8
JARVIS_PERSONALITY_PROACTIVITY=0.5
JARVIS_PERSONALITY_ADDRESS=sir
JARVIS_PERSONALITY_STYLE=concise

# Voice subsystem configuration
JARVIS_VOICE_ENABLED=false
JARVIS_VOICE_MODE=wake_word
JARVIS_VOICE_STT_PROVIDER=faster-whisper
JARVIS_VOICE_STT_MODEL=tiny.en
JARVIS_VOICE_STT_DEVICE=cpu
JARVIS_VOICE_TTS_PROVIDER=sapi5
JARVIS_VOICE_TTS_VOICE=Microsoft David Desktop
JARVIS_VOICE_TTS_SPEED=1.0
JARVIS_VOICE_VAD_ENABLED=true
JARVIS_VOICE_WAKE_WORD_PHRASE=jarvis

